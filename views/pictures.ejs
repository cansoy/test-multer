<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="./style.css">
    <title>Document</title>
</head>
<body>
    <hr>
    <hr>
    
    <% filterUser.forEach(element => { %>
        <ul>
            <li><%= element.name %></li>
            <li><%= element.surname %></li>
            <li><a href="/pictures/<%= element.filename %>"><%= element.filename %></a></li>
            <li><img src="data:image/png;base64,<%= element.base64  %>" width="200" height="200"></li>
            <hr>
        </ul>
    <% }) %>
</body>
</html>